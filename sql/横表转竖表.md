```
string sql = "SELECT exam_no,real_name,gender,age,idcard_no";
sql += ",MAX(CASE item_key WHEN 'urine_uro' THEN item_value ELSE '' END) AS urine_uro";
sql += ",MAX(CASE item_key WHEN 'urine_glu' THEN item_value ELSE '' END) AS urine_glu";
sql += " FROM ylyq_peis_exam_checkup GROUP BY exam_no";
```
